[Service]
# irlume bundles onnxruntime (Fedora ships 1.22; the ort api-24 ABI needs
# >=1.24). Point the dynamic loader at the bundled runtime.
Environment="ORT_DYLIB_PATH=/usr/share/irlume/onnxruntime/lib/libonnxruntime.so"
