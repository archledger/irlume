[package]
name = "irlume-fingerprint"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
irlume-common.workspace = true
# Detection/enroll-orchestration only: drives fprintd's CLI tools via
# std::process. Shared helper deadlines come from irlume-common; verification
# is done by pam_fprintd in the PAM stack,
# not here, so irlume never claims the sensor and coexists with the greeter.

[lints]
workspace = true
