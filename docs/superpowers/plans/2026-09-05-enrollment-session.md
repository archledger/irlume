# Bounded guided enrollment implementation

User approved proceeding with one approval, one initial countdown and continuous progress. Execute inline without subagents. Preserve the installed CLI/TUI and authorization candidates in their existing worktrees. New worktree perf/enrollment-session at 8bf99653 inherits the exact 36 changed files from the installed candidate.

1. Add an opt-in EnrollmentSession request (account, optional profile, count, improve flag), plus a typed stream envelope for progress and merge decisions. Existing Enroll/AddScan wire formats remain valid. Test raw new request parsing and count/subject authorization before capture. Client transport bounds each line, total duration and cancellation, and must never reconnect/resubmit a partially completed capture.
2. Extend the existing capture path with an enrollment observer. Keep legacy APIs as wrappers. Observer reports accepted scan progress and asks merge permission after the probe, before additional capture or publication. If the final target changes from the probe, require confirmation for that target before save. Cancellation, errors or preemption discard the candidate; the storage save remains the commit boundary. No scan/PAD thresholds, vote freshness policy or camera session lifetime changes.
3. Route the new request through the same posture, per-request polkit grant and camera arbiter. Connect reader and worker with bounded event/decision channels. Network writes stay off the camera worker. Merge wait is bounded and checks the existing cancellation/preemption token. Total operation budget remains bounded. Admission failures must not mutate storage. Tests cover unauthorized and changed-scope requests, malformed decisions, cancellation, timeout, backpressure and prompt refusal before publication.
4. TUI performs its initial framing/countdown once, then sends one bounded request. Show progress and a matching-profile prompt while keeping the same request alive. Stop/decline closes the operation, no compensating DeleteScan. Keep legacy behavior only through an explicit unsupported-protocol path, with no retry after an accepted new operation. Tests cover one request, continuous counts, literal target, merge yes/no, disconnect and stale results. CLI batch commands remain compatible.
5. Run affected common/CLI software checks locally; auth/daemon checks and all inference on archhost. Verify formatter, Clippy, docs and Rust1.88. Coordinate current installed TUI baseline and candidate live comparison on archhost before claiming speedup or deploying. Keep unsuccessful experiments and exact source/binary hashes. Report theoretical delay savings separately from actual timings.

Review boundaries: one account and fixed requested scan budget per authorization; no reusable authorization cache. Fresh presentation evidence and existing camera scheduling policy are preserved. No enrollment images, embeddings, scores or recovery material in artifacts. Confirm before saving a merge. No new backup required on archhost; previous rollback retained.
