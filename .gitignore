/target
**/*.rs.bk
Cargo.lock

# Never commit captured frames, templates, or any biometric data:
*.onnx.tmp
/var
*.embedding
*.template

# Model weights are ALL bundled in-repo via Git LFS (see .gitattributes +
# models/README.md) so a clone/package is self-contained. The one exception:
# the superseded v1 adapter backup, kept locally for revert, never shipped.
models/ir_adapter.onnx.v1-256
.deb-staging
irlume_*.deb
.arch-build
irlume-*.pkg.tar.zst
