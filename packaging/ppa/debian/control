Source: irlume
Section: admin
Priority: optional
Maintainer: Wisbendji Fimerlus <archledger236@gmail.com>
Build-Depends: debhelper-compat (= 13),
               cargo,
               rustc (>= 1.88),
               clang,
               libclang-dev,
               pkg-config,
               libpam0g-dev,
               libtss2-dev,
               libudev-dev,
               libdbus-1-dev,
               linux-libc-dev,
               cmake,
               extra-cmake-modules,
               qt6-base-dev,
               qt6-declarative-dev,
               libkf6kcmutils-dev,
               libkf6coreaddons-dev,
               libkf6kio-dev
Standards-Version: 4.6.2
Homepage: https://github.com/archledger/irlume
Vcs-Git: https://github.com/archledger/irlume.git
Vcs-Browser: https://github.com/archledger/irlume
Rules-Requires-Root: no

Package: irlume
Architecture: amd64
Depends: ${shlibs:Depends}, ${misc:Depends}, libpam0g, libpam-modules, polkitd | policykit-1
Recommends: fprintd
Description: Face authentication for Linux
 IR (Windows Hello) cameras get the secure tier: login, sudo, and TPM-sealed
 keyring unlock with algorithmic IR liveness. Regular RGB webcams get screen
 unlock; a fingerprint reader can join as a companion factor. Thin PAM module
 plus privileged daemon with face detection, recognition and liveness models.
 Password login remains available when face authentication is unavailable
 or retry-limited.
 .
 Bundles onnxruntime (>= 1.24) under /opt/irlume because irlume needs a newer
 runtime than the Ubuntu archive ships. Model weights install to
 /usr/share/irlume/models.

Package: irlume-kcm
Architecture: amd64
Depends: ${shlibs:Depends}, ${misc:Depends}, irlume (= ${binary:Version}),
         qml6-module-org-kde-kcmutils,
         qml6-module-org-kde-kirigami,
         qml6-module-qtquick-controls,
         qml6-module-qtquick-layouts,
         qml6-module-qtquick-templates,
         qml6-module-qtquick-window,
         qml6-module-qtqml-workerscript
Description: Plasma System Settings module for irlume
 Read-only status, diagnostics, camera census and login-wiring pages for
 irlume inside Plasma's System Settings, plus launch buttons that
 delegate every interactive or privileged change to the irlume terminal
 interface (with its own confirmations and authorizations). The module
 renders the same versioned machine API that external integrations use;
 it holds no policy and performs no privileged action. Requires a
 Plasma 6 session.
